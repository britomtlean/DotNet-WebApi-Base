

Manipulação de dados -- Mongo

.InsertOneAsync(); -- POST
.DeleteOneAsync(u => u.Id == id); -- DELETE
.Find(u => u.Id == id).FirstOrDefaultAsync(); -- GET/ID
.Find(_ => true).ToListAsync(); -- GET/ALL

----------------- PUT/UPDATE -----------------

var setUpdate = Builders<Entity>.Update
    .Set(e => e.Disponibilidade, update.Disponibilidade)
    .Set(e => e.Nome, update.Nome)
    .Set(e => e.Descricao, update.Descricao)
    .Set(e => e.Valor, update.Valor);

await _collectionEntity.UpdateOneAsync(
    u => u.User == user,
    setUpdate
);
----------------------------------------------
